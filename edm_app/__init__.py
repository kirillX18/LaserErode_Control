"""
edm_app — модульный интерфейс «Управление лазерно-эрозионным роботом».

Архитектура (компонентный + наследуемый подход):

    theme        — единый источник стилей и констант оформления
    components/   — мелкие переиспользуемые виджеты (кнопки, блоки шага, индикаторы)
    base.py       — базовые классы (BasePanel, BasePage, BaseServiceTab)
    blocks/       — крупные переиспользуемые панели (блок оси, блок сустава и т.п.)
    tabs/         — вкладки сервисного управления, собранные из блоков
    pages/        — страницы верхнего уровня (процесс, сервис)
    window.py     — главное окно, собирающее страницы

Чтобы добавить новую вкладку/страницу — достаточно унаследоваться от базового
класса и собрать содержимое из готовых блоков, не трогая остальной код.
"""

from .app import run

__all__ = ["run"]
